# Map Scripts

Scripts in the directory `data/scripts/map` are general purpose map scripts.
They are not executed directly by the core engine, but are instead called from
other scripts.

## Calling Convention

No special globals exist for map scripts.
